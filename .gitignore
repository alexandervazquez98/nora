# Carpeta de reportes históricos, capturas y scripts de prueba
.reportes/
.atl/

# Entornos virtuales de Python
env/
venv/
.venv/

# Archivos compilados y temporales
__pycache__/
*.py[cod]
*$py.class
*.so

# Toolchain caches and build artefacts
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/

# Variables de entorno y credenciales
.env
.env*
!.env.example
!.env.mcp.example
*.pem
*.key
id_*

# Logs y temporales
*.log
*.pid
*.sqlite3
*.db

# Operator-supplied device inventory (contains SNMP credentials).
# Issue #42 / `2026-09-15-register-device-mcp`: this MUST land in
# the same PR that introduces `register_device` so the existing
# public template (`data/devices.example.yaml`) is the only tracked
# file under `data/`.
data/devices.yaml
data/devices-*.yaml
!data/devices.example.yaml

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
